dependencies {
    testCompile project(':cubano-config')

	compile 'com.google.code.gson:gson:2.8.5'
	// Breaks plgins in Jenkins if go higher than this.  Only using it to get MediaType so probably not an issue
	compile 'com.google.guava:guava:19.0'
}