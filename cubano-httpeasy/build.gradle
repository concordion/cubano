dependencies {
    testCompile project(':cubano-config')

	compile 'com.google.code.gson:gson:2.8.5'
	// Is required when running in Jenkins, but nowhere else
	compile 'com.google.guava:guava:25.1-jre'
}